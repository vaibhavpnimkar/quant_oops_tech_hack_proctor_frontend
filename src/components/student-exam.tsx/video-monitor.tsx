import { ObjectDetection } from "../ai-validation/object-detection";

const VideoMonitor = () => {
  return <ObjectDetection className="min-w-[16rem] aspect-[3/2]" />;
};

export default VideoMonitor;
